name: Tests
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install modules
        run: npm i
      - name: generate types
        run: npm run gen
      - name: Run tests
        run: npm run all-tests
