version: "3.8"
services: 
  mongoDB:
    container_name: tuniDB
    image: mongo:5.0.5
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=root
    volumes:
      - /data/db
    ports:
      - '27017:27017'